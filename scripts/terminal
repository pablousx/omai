#!/bin/bash
set -uo pipefail
omai_root=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)
"$omai_root/scripts/omai" "$@"
omai_result=$?
printf '\nPress Enter to close.'
read -r _
exit "$omai_result"
